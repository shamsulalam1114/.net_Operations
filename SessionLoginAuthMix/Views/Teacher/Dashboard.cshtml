@model SessionLoginAuthMix.DTOs.UserDTO

@{
    ViewData["Title"] = "Teacher Dashboard";
}

<h2>Teacher Dashboard</h2>

<div class="card mt-3">
    <div class="card-body">
        <p><strong>Name:</strong> @Model.Name</p>
        <p><strong>Email:</strong> @Model.Email</p>
        <p><strong>Role:</strong> @Model.Role</p>
    </div>
</div>
